import { QueryInputDirective } from './query-input.directive';

describe('QueryInputDirective', () => {
  it('should create an instance', () => {
    const directive = new QueryInputDirective();
    expect(directive).toBeTruthy();
  });
});
