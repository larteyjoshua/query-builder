import { QueryArrowIconDirective } from './query-arrow-icon.directive';

describe('QueryArrowIconDirective', () => {
  it('should create an instance', () => {
    const directive = new QueryArrowIconDirective();
    expect(directive).toBeTruthy();
  });
});
