import { QueryFieldDirective } from './query-field.directive';

describe('QueryFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new QueryFieldDirective();
    expect(directive).toBeTruthy();
  });
});
