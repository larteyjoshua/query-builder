import { QueryOperatorDirective } from './query-operator.directive';

describe('QueryOperatorDirective', () => {
  it('should create an instance', () => {
    const directive = new QueryOperatorDirective();
    expect(directive).toBeTruthy();
  });
});
