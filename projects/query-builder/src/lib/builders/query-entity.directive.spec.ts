import { QueryEntityDirective } from './query-entity.directive';

describe('QueryEntityDirective', () => {
  it('should create an instance', () => {
    const directive = new QueryEntityDirective();
    expect(directive).toBeTruthy();
  });
});
